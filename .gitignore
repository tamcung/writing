__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.ruff_cache/
.mypy_cache/
.ipynb_checkpoints/
.DS_Store

# Local environments and secrets
.env
.venv/
venv/
env/

# Plugin source (installed separately)
.research-writing-skill/

# Data and third-party checkouts are synced separately.
data/raw/
data/processed/
data/derived/
data/splits/
data/pairs/
external/

# Experiment outputs can be large or machine-specific.
experiments/*.json
experiments/**/*.json
experiments/*.partial.json
*.tar
*.tar.gz
*.zip
# Exception: experiment input bundles are small (11MB+5.7MB) and are synced
# via the repo so that RunPod / CI can run `setup_exp{1,2}.sh` directly after
# `git clone` without a separate rsync step.
!experiment1_inputs.tar.gz
!experiment2_inputs.tar.gz

# Model checkpoints and binary artifacts
*.pt
*.pth
*.ckpt
*.safetensors
*.dump
*.joblib
*.pkl
*.tsbuildinfo

# System build outputs
system/backend/storage/
system/frontend/node_modules/
system/frontend/dist/
system/runtime/
